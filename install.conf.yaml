- defaults:
   link:
      create: true
      relink: true

- clean: ['~']

- link:
      ~/.dotfiles-local: ''
      ~/.config/autorandr:
      ~/.config/compton.conf:
      ~/.config/i3:
      ~/.config/polybar:

- shell: []
